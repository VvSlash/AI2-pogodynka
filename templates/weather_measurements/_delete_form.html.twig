<form method="post" action="{{ path('app_weather_measurements_delete', {'id': weather_measurement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ weather_measurement.id) }}">
    <button class="btn">Delete</button>
</form>
